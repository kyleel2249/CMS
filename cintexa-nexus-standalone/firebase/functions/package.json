{
  "name": "cintexa-nexus-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions — wraps the Express API server",
  "type": "module",
  "main": "index.mjs",
  "engines": { "node": "20" },
  "scripts": {
    "build": "echo 'No build step — uses pre-built bundle'",
    "serve": "firebase emulators:start --only functions,hosting"
  },
  "dependencies": {
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "firebase-admin": "^13.0.0"
  }
}
